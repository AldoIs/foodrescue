from django.urls import path

urlpatterns = [
    # Define aquí tus patrones de URL para CRUD de cada modelo,
    # por ejemplo:
    # path('proveedores/', ProveedorList.as_view(), name='proveedor-list'),
]
